// I had to create a color class because apparently processing doesn't allow
// java generics to be used on this type

class Color {
  color aColor;
 
  Color(color aColor) {
    this.aColor = aColor;
  }
}
